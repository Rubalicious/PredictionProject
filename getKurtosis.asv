function x = getKurtosis(I)
    
    
    disp(size(
    A = zeros(size(I,1)*size(I,2));
    i = 1;
    for r = 1:size(I,1)
        for c = 1:size(I,2)
            A(i) = I(r,c);
        end
    end
    
    x = kurtosis(A);
end